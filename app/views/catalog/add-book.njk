<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" type="text/css" href="/css/add-book.css" />
</head>
<body>
    <main class='books-app'>
        <h1>Add a book</h1>
        <form action="/catalog/book" method="post" class='form'>
            <label>
                    Author
                    <select name="author">
                        {% for author in authors %}
                            <option value="{{ author._id }}">
                                {{author.title}} {{ author.firstName }} {{ author.lastName}}
                            </option>
                        {% endfor %}
                    </select>
            </label>
            <small>
                Can't find the author?
                <a href='/catalog/author/add'>Add Author</a>
            </small>
            <label>
                    Book title
                    <input name="title" type="text">
            </label>
            <button type="submit" value="add-book">
                Add Book
            </button>
        </form>
    </main>
</body>
</html>
